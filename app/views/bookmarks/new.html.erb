<div class="container mt-3">
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment %>
    <%= f.submit %>
  <% end %>
  <%= link_to 'Back', list_path(@list.id) %>
</div>
