<div class="container mt-3">
  <%= simple_form_for [@list, @bookmark] do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment %>
    <%= f.submit class: "btn btn-outline-primary" %>
    <%= link_to 'Back', list_path(@list.id), class: "btn btn-outline-dark" %>
  <% end %>
</div>
